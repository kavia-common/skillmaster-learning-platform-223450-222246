# Frontend environment configuration (public - safe to share)
# API base URL for backend (FastAPI). In preview, defaults to http://localhost:3001 if not set.
REACT_APP_BACKEND_URL=http://localhost:3001

# Optional additional configs:
# REACT_APP_API_BASE=
# REACT_APP_FEATURE_FLAGS=
# REACT_APP_LOG_LEVEL=info
# REACT_APP_FRONTEND_URL=http://localhost:3000
